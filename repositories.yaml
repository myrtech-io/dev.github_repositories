repositories:
  - name: github_repositories
    description: Github Repositories
    branch_prefixes: ["master", "feature", "release"]

  - name: resumate
    description: Resume builder repository
    branch_prefixes: ["master", "feature", "release"]

  - name: kubernetes_installation
    description: Multinode kubernetes installation using ansible
    branch_prefixes: ["master", "feature", "release"]
    
